services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/robinvdveer/test2-web:latest
    pull_policy: build
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
