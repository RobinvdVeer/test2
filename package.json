{
  "name": "really-bad-chess-web-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check public/app.js && node --check public/chess-engine.js && node --check bot.js && node --check public/audio.js",
    "test": "node --test"
  }
}
